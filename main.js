document.addEventListener('DOMContentLoaded', function(){ const featured=document.getElementById('featuredGrid'); if(featured){ PRODUCTS.slice(0,4).forEach(p=>{ const c=document.createElement('div'); c.className='card'; c.innerHTML=`<img src="${p.img}" alt="${p.title}"><h4>${p.title}</h4><p>PKR ${p.price}</p><p>${p.desc}</p>`; featured.appendChild(c); }); } });